ROLE=OBSERVER
# OPTIONS:
# OBSERVER  → map / view / mirror only (NO execution)
# EXECUTOR  → allowed to run carts & emoji operations
# ARCHIVE   → memory storage only
# SPAWNER   → allowed to create new repos
