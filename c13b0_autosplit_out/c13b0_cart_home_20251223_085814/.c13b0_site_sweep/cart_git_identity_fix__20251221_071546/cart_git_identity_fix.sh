#!/usr/bin/env bash
set -e
git config --global user.name "Kris"
git config --global user.email "marvaseater@gmail.com"
echo "[∞] git identity set:"
git config --global user.name
git config --global user.email
